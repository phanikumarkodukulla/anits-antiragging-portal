<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ANITS | Complaint Submitted Successfully</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    />
    <link rel="stylesheet" href="/css/style.css" />

    <style>
      .success-animation {
        animation: scaleIn 0.5s ease-out;
      }
      @keyframes scaleIn {
        from {
          transform: scale(0);
          opacity: 0;
        }
        to {
          transform: scale(1);
          opacity: 1;
        }
      }
      .complaint-id-box {
        background: linear-gradient(135deg, var(--primary-blue), #1e4a7a);
        color: white;
        padding: 2rem;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(42, 89, 155, 0.3);
      }
      .timeline {
        position: relative;
      }
      .timeline-step {
        display: flex;
        gap: 20px;
        margin-bottom: 30px;
        position: relative;
      }
      .timeline-step:not(:last-child)::before {
        content: "";
        position: absolute;
        left: 23px;
        top: 50px;
        width: 2px;
        height: calc(100% - 20px);
        background: #dee2e6;
      }
      .timeline-step.completed::before {
        background: var(--success);
      }
      .timeline-icon {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        background: #e9ecef;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        font-size: 1.25rem;
        color: #6c757d;
        position: relative;
        z-index: 1;
      }
      .timeline-step.completed .timeline-icon {
        background: var(--success);
        color: white;
      }
      .timeline-content {
        flex: 1;
        padding-top: 8px;
      }

      @media print {
        .btn,
        button,
        nav,
        footer,
        .sos-button {
          display: none !important;
        }
      }
    </style>
  </head>
  <body class="student-page">
    <%- include('../partials/navbar', { currentPage: '' }) %>

    <main class="container section-padding">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <!-- Success Message -->
          <div class="text-center mb-5 success-animation">
            <div class="mb-4">
              <i
                class="fas fa-check-circle text-success"
                style="font-size: 6rem"
              ></i>
            </div>
            <h1 class="fw-bold text-success mb-3">
              Complaint Submitted Successfully!
            </h1>
            <p class="lead text-muted">
              Your complaint has been received and will be reviewed by our team
              within 24 hours.
            </p>
          </div>

          <!-- Complaint ID Card -->
          <div class="complaint-id-box mb-4 text-center">
            <p class="mb-2" style="opacity: 0.9">Your Complaint ID</p>
            <h2
              class="fw-bold font-monospace mb-3"
              style="letter-spacing: 3px; font-size: 2rem"
            >
              <%= complaint.complaintId %>
            </h2>
            <p class="mb-0" style="opacity: 0.8">
              <i class="fas fa-info-circle me-2"></i>Please save this ID to
              track your complaint status
            </p>
          </div>

          <!-- Complaint Summary -->
          <div class="card shadow-sm mb-4">
            <div class="card-header">
              <h5 class="mb-0">
                <i class="fas fa-file-alt me-2"></i>Complaint Summary
              </h5>
            </div>
            <div class="card-body">
              <div class="row g-3">
                <div class="col-md-6">
                  <div class="p-3 border rounded">
                    <small class="text-muted d-block mb-1"
                      ><i class="fas fa-calendar me-1"></i>Submitted On</small
                    >
                    <strong
                      ><%= new
                      Date(complaint.createdAt).toLocaleDateString('en-US', {
                      year: 'numeric', month: 'long', day: 'numeric', hour:
                      '2-digit', minute: '2-digit' }) %></strong
                    >
                  </div>
                </div>
                <div class="col-md-6">
                  <div class="p-3 border rounded">
                    <small class="text-muted d-block mb-1"
                      ><i class="fas fa-info-circle me-1"></i>Status</small
                    >
                    <span class="badge bg-warning">Pending Review</span>
                  </div>
                </div>
                <div class="col-md-6">
                  <div class="p-3 border rounded">
                    <small class="text-muted d-block mb-1"
                      ><i class="fas fa-exclamation-triangle me-1"></i>Severity
                      Level</small
                    >
                    <% let severityClass = 'warning'; if (complaint.severity ===
                    'High') severityClass = 'danger'; if (complaint.severity ===
                    'Low') severityClass = 'success'; %>
                    <span class="badge bg-<%= severityClass %>"
                      ><%= complaint.severity %></span
                    >
                  </div>
                </div>
                <div class="col-md-6">
                  <div class="p-3 border rounded">
                    <small class="text-muted d-block mb-1"
                      ><i class="fas fa-map-marker-alt me-1"></i>Location</small
                    >
                    <strong><%= complaint.location %></strong>
                  </div>
                </div>
              </div>

              <div class="mt-4">
                <h6 class="fw-bold mb-2">Complaint Title</h6>
                <p class="mb-0"><%= complaint.title %></p>
              </div>
            </div>
          </div>

          <!-- What Happens Next -->
          <div class="card shadow-sm mb-4">
            <div class="card-header" style="background-color: #f8f9fa">
              <h5 class="mb-0">
                <i class="fas fa-clock me-2"></i>What Happens Next?
              </h5>
            </div>
            <div class="card-body">
              <div class="timeline">
                <div class="timeline-step completed">
                  <div class="timeline-icon">
                    <i class="fas fa-check"></i>
                  </div>
                  <div class="timeline-content">
                    <h6 class="fw-bold mb-1">Complaint Submitted</h6>
                    <p class="text-muted small mb-0">
                      Your complaint has been securely recorded in our system
                    </p>
                  </div>
                </div>
                <div class="timeline-step">
                  <div class="timeline-icon">
                    <i class="fas fa-search"></i>
                  </div>
                  <div class="timeline-content">
                    <h6 class="fw-bold mb-1">Under Review</h6>
                    <p class="text-muted small mb-0">
                      Admin team will review and assess the complaint
                    </p>
                  </div>
                </div>
                <div class="timeline-step">
                  <div class="timeline-icon">
                    <i class="fas fa-gavel"></i>
                  </div>
                  <div class="timeline-content">
                    <h6 class="fw-bold mb-1">Investigation & Action</h6>
                    <p class="text-muted small mb-0">
                      Appropriate action will be taken based on findings
                    </p>
                  </div>
                </div>
                <div class="timeline-step">
                  <div class="timeline-icon">
                    <i class="fas fa-check-circle"></i>
                  </div>
                  <div class="timeline-content">
                    <h6 class="fw-bold mb-1">Resolution</h6>
                    <p class="text-muted small mb-0">
                      You'll be notified once the case is resolved
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Action Buttons -->
          <div class="row g-3 mb-4">
            <div class="col-md-4">
              <a href="/complaint/track" class="btn btn-primary w-100 btn-lg">
                <i class="fas fa-search me-2"></i>Track Complaint
              </a>
            </div>
            <div class="col-md-4">
              <button
                onclick="window.print()"
                class="btn btn-outline-primary w-100 btn-lg"
              >
                <i class="fas fa-print me-2"></i>Print Receipt
              </button>
            </div>
            <div class="col-md-4">
              <a href="/" class="btn btn-outline-secondary w-100 btn-lg">
                <i class="fas fa-home me-2"></i>Go to Home
              </a>
            </div>
          </div>

          <!-- Important Notes -->
          <div class="card" style="border-left: 4px solid var(--primary-blue)">
            <div class="card-body">
              <h6 class="fw-bold mb-3">
                <i class="fas fa-info-circle me-2"></i>Important Notes
              </h6>
              <ul class="mb-0">
                <li class="mb-2">
                  You will receive updates via email and SMS at the contact
                  information you provided
                </li>
                <li class="mb-2">
                  Response time is typically within 24 hours during working days
                </li>
                <li class="mb-2">
                  You can track your complaint anytime using your Complaint ID
                </li>
                <li>
                  If you need immediate assistance, please call our emergency
                  helpline: <strong>1800-180-5522</strong>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include('../partials/footer') %>

    <!-- SOS Button -->
    <button id="sosButton" class="sos-button" title="Emergency SOS Alert">
      <i class="fas fa-exclamation-triangle"></i>
    </button>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    <script src="/js/sos.js"></script>
  </body>
</html>
